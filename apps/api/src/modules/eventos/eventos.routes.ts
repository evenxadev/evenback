import { FastifyInstance } from "fastify";

export async function eventosRoutes(app: FastifyInstance) {
  // GET    /eventos
  // GET    /eventos/:id
  // POST   /eventos       (admin, event_manager)
  // PUT    /eventos/:id
  // DELETE /eventos/:id
}