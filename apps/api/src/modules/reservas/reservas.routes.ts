import { FastifyInstance } from "fastify";

export async function reservasRoutes(app: FastifyInstance) {
  // POST /reservas               comprar boleto
  // GET  /reservas/mis-boletos
  // GET  /reservas/:id
}